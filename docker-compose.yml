services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: webchatt
      POSTGRES_PASSWORD: lemotdepasse
      POSTGRES_DB: chat
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
